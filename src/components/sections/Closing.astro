---
import { resolveUrl } from '../../utils.mjs';
const { data } = Astro.props;
---
<section class="act" id="book">
  <div class="wrap act__inner">
    <h2>{data.heading}</h2>
    <p>{data.body}</p>
    <div class="actions">
      <a class="btn" href={resolveUrl(data.cta.url)}>{data.cta.label}</a>
    </div>
  </div>
</section>
